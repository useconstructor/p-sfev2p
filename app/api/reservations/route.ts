import { db } from '@/lib/db'
export async function POST(request:Request){const b=await request.json();const{nombre,email,telefono,fecha,hora,personas,solicitudes}=b;if(!nombre||!email||!telefono||!fecha||!hora||!personas)return Response.json({error:'Faltan datos'},{status:400});await db.execute(`CREATE TABLE IF NOT EXISTS reservations (id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL,email TEXT NOT NULL,phone TEXT NOT NULL,booking_date TEXT NOT NULL,booking_time TEXT NOT NULL,guests INTEGER NOT NULL,requests TEXT,created_at TEXT DEFAULT (datetime('now')))`);await db.execute({sql:'INSERT INTO reservations (name,email,phone,booking_date,booking_time,guests,requests) VALUES (?,?,?,?,?,?,?)',args:[nombre,email,telefono,fecha,hora,Number(personas),solicitudes??'']});return Response.json({ok:true},{status:201})}
